/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['example.com', 'api.react-finland.fi'],
    },
  };
  
  export default nextConfig;
